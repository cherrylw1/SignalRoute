import type { Metadata } from "next";
import { PageShell } from "@/components/page-shell";

export const metadata: Metadata = { title: "Careers", description: "Future opportunities to help build Churnaut." };
const roles = [{title:"Sales Development Representative",focus:"Build carefully targeted outbound, qualify interest and help sharpen the first customer segment.",subject:"SDR expression of interest"},{title:"Customer Success Manager",focus:"Own founder-adjacent onboarding, help customers launch their first signals and turn feedback into better product decisions.",subject:"CSM expression of interest"}];
export default function CareersPage() { return <PageShell><section className="page-hero"><div className="page-hero-inner"><p className="eyebrow">JOIN CHURNAUT</p><h1 className="display">Help make B2B websites<br />feel more <em>attentive.</em></h1><p className="lede">We are not actively hiring today. We are building a small list of thoughtful people in India who may want to join when the first roles open.</p></div></section><section className="section-pad"><div className="split-heading"><div><p className="eyebrow">FUTURE ROLES</p><h2 className="section-title">Small team.<br />Real <em>ownership.</em></h2></div><p className="section-copy">These are expressions of interest, not current employment offers. Churnaut is early-stage and still completing its company formation.</p></div><div className="feature-grid">{roles.map((role,index)=><article className="feature-card" key={role.title}><span>0{index+1} · REMOTE / INDIA</span><h3>{role.title}</h3><p>{role.focus}</p><a className="button button-outline" style={{marginTop: "20px"}} href={`mailto:careers@churnaut.com?subject=${encodeURIComponent(role.subject)}`}>Introduce yourself <span>→</span></a></article>)}</div></section></PageShell>; }
